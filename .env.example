# memU Production Environment Configuration
# Copy this file to .env and fill in your actual values

# =============================================================================
# API Keys
# =============================================================================

# OpenAI API Key (https://platform.openai.com/api-keys)
# Required if using OpenAI models
OPENAI_API_KEY=sk-your-openai-api-key-here

# OpenRouter API Key (https://openrouter.ai/keys)
# Required if using OpenRouter for model access
OPENROUTER_API_KEY=your-openrouter-api-key-here

# OpenRouter Base URL
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1

# =============================================================================
# Ollama Configuration
# =============================================================================

# Ollama API Base URL
# For production deployment on the same host as Ollama
# Use the host IP address and port (default: 11434)
OLLAMA_API_BASE_URL=http://192.168.25.165:11434/api

# =============================================================================
# Database Configuration
# =============================================================================

# PostgreSQL Database Name
POSTGRES_DB=memu_db

# PostgreSQL User
# IMPORTANT: Change this from the default for production
POSTGRES_USER=memu_production_user

# PostgreSQL Password
# IMPORTANT: Use a strong random password (32+ characters recommended)
# Generate with: openssl rand -base64 32
POSTGRES_PASSWORD=your-strong-random-password-here

# Database Connection URL
# Update the password to match POSTGRES_PASSWORD above
MEMU_DB_URL=postgresql+psycopg://memu_production_user:your-strong-random-password-here@db:5432/memu_db

# =============================================================================
# Resource Limits (Optional)
# =============================================================================

# Memory limit for memU application container
# Adjust based on your workload and available system memory
MEMU_MEMORY_LIMIT=2g

# Memory limit for PostgreSQL database container
# Adjust based on your workload and available system memory
DB_MEMORY_LIMIT=1g
